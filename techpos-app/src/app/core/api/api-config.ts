// Backend NestJS corre en :3000 (CORS habilitado en main.ts). Si más adelante
// se prefiere evitar CORS, reemplazar por un proxy.conf.json y usar '/api'.
export const API_BASE_URL = 'http://localhost:3000';
